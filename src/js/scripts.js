//===============================================
function callServer(_module, _method) {
    var lServer = new GServer();
    lServer.run(_module, _method);
}
//===============================================
