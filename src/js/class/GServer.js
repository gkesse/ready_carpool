//===============================================
class GServer extends GObject {
    //===============================================
    constructor() {
        super();
    }
    //===============================================
    run(_module, _method) {
        if(_module == "test") {
            this.runTest(_module, _method);
        }
    }
    //===============================================
    runTest(_module, _method) {
        var lObj = new GTest();
        lObj.run(_module, _method);
    }
    //===============================================
}
//===============================================
