//===============================================
// BackgroundMod1
//===============================================
function openBackgroundMod1(obj) {
	GEdition.Instance().openBackgroundMod1(obj);
}
//===============================================
function closeBackgroundMod1(obj) {
	GEdition.Instance().closeBackgroundMod1(obj);
}
//===============================================
function saveBackgroundMod1(obj) {
	GEdition.Instance().saveBackgroundMod1(obj);
}
//===============================================
// BackgroundMod1Modify
//===============================================
function openBackgroundMod1Modify(obj) {
	GEdition.Instance().openBackgroundMod1Modify(obj);
}
//===============================================
function closeBackgroundMod1Modify(obj) {
	GEdition.Instance().closeBackgroundMod1Modify(obj);
}
//===============================================
function saveBackgroundMod1Modify(obj) {
	GEdition.Instance().saveBackgroundMod1Modify(obj);
}
//===============================================
function openFile(obj, type, name, filename) {
	GEdition.Instance().openFile(obj, type, name, filename);
}
//===============================================
function openLink(obj, index) {
	GEdition.Instance().openLink(obj, index);
}
//===============================================
// BackgroundMod2
//===============================================
function openBackgroundMod2(obj) {
	GEdition.Instance().openBackgroundMod2(obj);
}
//===============================================
function closeBackgroundMod2(obj) {
	GEdition.Instance().closeBackgroundMod2(obj);
}
//===============================================
function saveBackgroundMod2(obj) {
	GEdition.Instance().saveBackgroundMod2(obj);
}
//===============================================
// BackgroundMod3
//===============================================
function openBackgroundMod3(obj) {
	GEdition.Instance().openBackgroundMod3(obj);
}
//===============================================
function closeBackgroundMod3(obj) {
	GEdition.Instance().closeBackgroundMod3(obj);
}
//===============================================
function saveBackgroundMod3(obj) {
	GEdition.Instance().saveBackgroundMod3(obj);
}
//===============================================
// HomeText
//===============================================
function openHomeText(obj) {
	GEdition.Instance().openHomeText(obj);
}
//===============================================
function closeHomeText(obj) {
	GEdition.Instance().closeHomeText(obj);
}
//===============================================
function saveHomeText(obj) {
	GEdition.Instance().saveHomeText(obj);
}
//===============================================